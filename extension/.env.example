# Copy to extension/.env before building the extension for a deployment.
# The URL origin must also be listed in package.json > manifest.host_permissions.
PLASMO_PUBLIC_BACKEND_URL=https://your-api.onrender.com
PLASMO_PUBLIC_GITHUB_CLIENT_ID=your-github-oauth-client-id
